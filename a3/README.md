# Assessment 3 - SkillSwap (A3)

Live site: <ADD YOUR CORETEACHING URL HERE>

This directory contains the Assignment 3 implementation. Work only occurs in `a3`.

- Tech: PHP (procedural, MySQLi), MySQL, Bootstrap 5
- Common includes in `includes/`
- Assets in `assets/`
- Uploads saved to `assets/images/skills/` (ignored by Git)

Database
- Put your `skillswap.sql` (base) and `skillswap_update.sql` (alterations) inside `a3/db/`
- See `a3/db/README_DB.md` for import steps (localhost and Jacob 5)

Deployment notes
- DB on Jacob 5 is named exactly your student ID
- Set folder perms: `755` for `a3` and `777` for `a3/assets/images/skills`
- Update nothing in `a1` or `a2`
